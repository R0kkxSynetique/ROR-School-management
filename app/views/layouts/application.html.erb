<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "School Management" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.13.7/css/jquery.dataTables.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/select/1.7.0/css/select.dataTables.min.css">
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.js"></script>
    <script src="https://cdn.datatables.net/select/1.7.0/js/dataTables.select.min.js"></script>
    <style>
      .dropdown-menu {
        display: none;
      }
      .dropdown-menu.show {
        display: block;
      }
      
      /* DataTables Custom Styling */
      .dataTables_wrapper {
        padding: 1rem;
        background: #ffffff;
        border-radius: 0.5rem;
        color: #1f2937;
      }
      .dataTables_filter input, .dataTables_length select {
        padding: 0.5rem;
        border: 1px solid #e5e7eb;
        border-radius: 0.375rem;
        margin-left: 0.5rem;
        color: #1f2937;
      }
      .dataTables_filter input:focus, .dataTables_length select:focus {
        outline: 2px solid #6366f1;
        outline-offset: 2px;
      }
      table.dataTable thead th {
        background-color: #f9fafb;
        padding: 1rem;
        text-align: left;
        color: #4b5563;
      }
      table.dataTable tbody td {
        padding: 1rem;
        color: #1f2937;
      }
      table.dataTable tbody tr:hover {
        background-color: #f3f4f6;
      }
      table.dataTable tbody tr.selected {
        background-color: #f3f4f6;
      }
      .dataTables_info, .dataTables_paginate {
        color: #1f2937 !important;
      }
      .paginate_button {
        color: #1f2937 !important;
      }
      .paginate_button.current {
        background: #6366f1 !important;
        color: #ffffff !important;
        border-color: #6366f1 !important;
      }
      .dataTables_length label, .dataTables_filter label {
        color: #1f2937;
      }
      
      table.dataTable tr.selected td.select-checkbox:after,
      table.dataTable tr.selected th.select-checkbox:after {
        content: "✓";
        margin-top: -11px;
        margin-left: -4px;
        text-align: center;
        text-shadow: 1px 1px #B0BED9, -1px -1px #B0BED9, 1px -1px #B0BED9, -1px 1px #B0BED9;
      }
      
      table.dataTable td.select-checkbox:before,
      table.dataTable th.select-checkbox:before {
        content: " ";
        margin-top: -6px;
        margin-left: -6px;
        border: 1px solid #000;
        border-radius: 3px;
        position: absolute;
        top: 50%;
        left: 50%;
        width: 12px;
        height: 12px;
        box-sizing: border-box;
        background-color: white;
      }
      
      table.dataTable td.select-checkbox,
      table.dataTable th.select-checkbox {
        position: relative;
        cursor: pointer;
      }
    </style>
    <script>
      document.addEventListener('click', function(event) {
        const dropdowns = document.querySelectorAll('.dropdown');
        dropdowns.forEach(dropdown => {
          const button = dropdown.querySelector('button');
          const menu = dropdown.querySelector('.dropdown-menu');
          
          if (event.target === button || button.contains(event.target)) {
            // Toggle dropdown on button click
            menu.classList.toggle('show');
            event.stopPropagation();
          } else if (!menu.contains(event.target)) {
            // Close dropdown when clicking outside
            menu.classList.remove('show');
          }
        });
      });
    </script>
  </head>
  <body class="min-h-screen text-gray-100 bg-gray-900">
    <nav class="bg-gray-800 shadow-lg">
      <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex items-center flex-shrink-0">
              <%= link_to root_path, class: "text-xl font-bold text-white" do %>
                School Management
              <% end %>
            </div>
            <div class="hidden space-x-4 sm:ml-6 sm:flex sm:items-center">
              <% if user_signed_in? %>
                <% if current_user.person.is_a?(Student) %>
                  <%= link_to "My Grades", users_grades_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <% elsif current_user.person.is_a?(Employee) %>
                  <% if current_user.person.is_a?(Dean) %>
                    <%= link_to "Dashboard", users_dean_dashboard_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                    <div class="relative dropdown">
                      <button type="button" class="flex items-center px-3 py-2 text-sm font-medium text-gray-300 rounded-md hover:text-white">
                        Manage
                        <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
                        </svg>
                      </button>
                      <div class="absolute left-0 z-50 w-48 py-2 mt-1 bg-gray-800 rounded-md shadow-xl dropdown-menu">
                        <%= link_to "Classes", users_dean_school_classes_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Courses", courses_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Periods", users_dean_periods_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Promotion Assertments", promotion_asserments_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Report Cards", users_dean_report_cards_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Schedules", users_dean_schedules_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Sections", users_dean_sections_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Specializations", users_dean_specializations_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Students", users_dean_students_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                        <%= link_to "Teachers", users_dean_teachers_path, class: "block px-4 py-2 text-sm text-gray-300 hover:text-white hover:bg-gray-700" %>
                      </div>
                    </div>
                  <% end %>
                  <%= link_to "My Examinations", users_examinations_path, class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
                <% end %>
              <% end %>
            </div>
          </div>
          <div class="flex items-center">
            <% if user_signed_in? %>
              <div class="flex items-center space-x-4">
                <span class="text-sm text-gray-300"><%= current_user.email %></span>
                <%= button_to 'Sign out', destroy_user_session_path, 
                    method: :delete, 
                    class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium" %>
              </div>
            <% else %>
              <div class="flex items-center space-x-4">
                <%= link_to 'Sign in', new_user_session_path, 
                    class: "text-gray-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <div class="fixed top-0 left-0 right-0 z-50 mt-16">
      <% if notice %>
        <div class="p-4 mx-4 mt-4 text-sm text-green-400 transition-all duration-500 bg-green-900 rounded-lg shadow-lg">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="p-4 mx-4 mt-4 text-sm text-red-400 transition-all duration-500 bg-red-900 rounded-lg shadow-lg">
          <%= alert %>
        </div>
      <% end %>
    </div>
    <main class="container px-4 py-8 mx-auto mt-16 sm:px-6 lg:px-8">
      <%= yield %>
    </main>
  </body>
</html>
