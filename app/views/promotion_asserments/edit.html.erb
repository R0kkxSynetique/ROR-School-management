<% content_for :title, "Edit #{@promotion_asserment.name}" %>

<div class="container mx-auto px-4 py-8">
  <div class="max-w-3xl mx-auto">
    <div class="mb-8">
      <h1 class="text-3xl font-bold leading-tight bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-purple-400">
        Edit Promotion Assessment
      </h1>
      <p class="mt-2 text-sm text-gray-400">Update the promotion assessment details and conditions</p>
    </div>

    <%= render 'form', promotion_asserment: @promotion_asserment %>
  </div>
</div>
