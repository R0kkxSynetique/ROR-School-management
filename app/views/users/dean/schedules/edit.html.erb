<% content_for :title, "Edit Schedule" %>

<div class="container mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent">Edit Schedule</h1>
    <p class="mt-2 text-gray-400">Modify the existing schedule details.</p>
  </div>

  <%= render 'form' %>
</div>
