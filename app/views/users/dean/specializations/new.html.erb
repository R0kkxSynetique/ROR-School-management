<% content_for :title, "New Specialization" %>

<div class="container mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent">New Specialization</h1>
    <p class="mt-2 text-gray-400">Create a new specialization for your school.</p>
  </div>

  <%= render 'form' %>
</div>
