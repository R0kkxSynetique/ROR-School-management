<% content_for :title, "New Period" %>

<div class="container mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold leading-tight bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-purple-400">
      New Period
    </h1>
    <p class="mt-2 text-sm text-gray-400">Create a new school period</p>
  </div>

  <%= render 'form' %>
</div>
