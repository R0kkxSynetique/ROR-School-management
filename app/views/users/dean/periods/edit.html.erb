<% content_for :title, "Edit Period" %>

<div class="container mx-auto px-4 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold leading-tight bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-purple-400">
      Edit Period
    </h1>
    <p class="mt-2 text-sm text-gray-400">Update period details</p>
  </div>

  <%= render 'form' %>
</div>
