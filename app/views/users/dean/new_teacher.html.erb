<% content_for :title, "Add New Teacher" %>

<div class="container px-4 py-8 mx-auto">
  <div class="max-w-2xl mx-auto">
    <div class="mb-8">
      <h1 class="text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-purple-400">Add New Teacher</h1>
      <p class="mt-2 text-sm text-gray-400">Create a new teacher record with their personal and contact information.</p>
    </div>

    <%= form_with(model: @teacher, url: users_dean_create_teacher_path, class: "space-y-6") do |f| %>
      <%= render partial: "teacher_form", locals: { f: f } %>
    <% end %>
  </div>
</div>
