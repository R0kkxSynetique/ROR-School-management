<% content_for :title, "New Examination" %>

<div class="container px-4 py-8 mx-auto">
  <div class="max-w-2xl mx-auto">
    <div class="mb-8">
      <h1 class="text-3xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-purple-400">New Examination</h1>
      <p class="mt-2 text-sm text-gray-400">Create a new examination record.</p>
    </div>

    <%= render "form", examination: @examination %>
  </div>
</div>
